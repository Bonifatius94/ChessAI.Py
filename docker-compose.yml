
version: "2"

services:

  chessai:
    image: chessai-train
    build:
      context: .
    entrypoint: ["python", "main.py"]
    volumes:
      - './model_weights:/app/model'
      - './logs:/app/logs'
    mem_limit: 3g
